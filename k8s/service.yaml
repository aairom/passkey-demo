apiVersion: v1
kind: Service
metadata:
  name: passkey-demo-service
  namespace: passkey-demo
  labels:
    app: passkey-demo
spec:
  type: ClusterIP
  selector:
    app: passkey-demo
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  sessionAffinity: None

# Made with Bob
