apiVersion: v1
kind: ConfigMap
metadata:
  name: passkey-demo-config
  namespace: passkey-demo
  labels:
    app: passkey-demo
data:
  RP_ID: "passkey.example.com"
  RP_ORIGIN: "https://passkey.example.com"
  PORT: "8080"

# Made with Bob
